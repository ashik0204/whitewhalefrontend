[build]
  base = "./"
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm run dev"

# Redirect all routes to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
